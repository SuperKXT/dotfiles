# Pop Launcher Custom Plugins

## Installation

1. Create a symlink to the plugin folder in `~/.local/share/pop-launcher/plugins/`

```bash
ln -sfT ~/dotfiles/config/pop-launcher/code-plugin ~/.local/share/pop-launcher/plugins/code-plugin
```
